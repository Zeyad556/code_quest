import 'package:flutter/material.dart';

class LessonsModel extends StatelessWidget {
  const LessonsModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
